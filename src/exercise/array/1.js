/**
 * @param {number[]} nums
 * @return {number}
 */
var removeDuplicates = function(nums) {
    if (nums.length < 2) return nums.length
    var index = 1;
    var cur;
    var last = nums[0];
    while (index < nums.length) {
        cur = nums[index];
        if ()
    }
};